# Base institution URL (without the API version)
INSTITUTION_URL=https://haskoliislands.instructure.com

# API version (e.g., v1 for current API version)
API_VERSION=v1

# API token for authentication (replace with your actual token)
API_TOKEN=your_actual_api_token

# Canvas course ID (replace with the actual course ID)
COURSE_ID=your_actual_course_id

# Date range for calendar event management
START_DATE=2025-01-01
END_DATE=2025-12-31
